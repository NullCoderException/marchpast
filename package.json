{
  "name": "marchpast",
  "version": "0.3.0",
  "private": true,
  "description": "Animated 2D playbacks of historic battles, driven by a reusable JSON timeline format.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "validate": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/validate.ts"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.8",
    "@types/node": "^26.4.1",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
